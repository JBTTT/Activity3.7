apiVersion: apps/v1
kind: Deployment
metadata:
  name: jibin-nginx-deployment
  namespace: jibin-eks-activity
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      serviceAccountName: jibin-nginx-service-account
      containers:
      - name: nginx
        image: nginx:latest
        ports:
        - containerPort: 80
