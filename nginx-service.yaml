apiVersion: v1
kind: Service
metadata:
  name: jibin-nginx-service
  namespace: jibin-eks-activity
spec:
  type: LoadBalancer
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
